int main()
{
    // Задаем первое и последнее числа в диапазоне
    int a1 = 50,
        an = 52,
    // Ищем количество чисел в диапазоне
        n = an - a1 + 1,
    // Находим сумму чисел в заданном диапазоне
        S = ((a1 + an)*n) / 2;

    return 0;
}
